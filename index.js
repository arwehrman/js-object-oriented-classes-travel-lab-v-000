class Driver {
  constructor(name, startdate) {
    this.name = name;
    this.startDate = new Date(startDate);
  }

  startDate(date) {
    return date.Date();
  }

  yearsExperienceFromBeginningOf() {
    
  }
}
